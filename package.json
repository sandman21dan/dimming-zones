{
  "name": "dimming-zones",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/index.html --public-url /dimming-zones/",
    "start": "parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.16.1",
    "gh-pages": "^6.3.0"
  }
}
